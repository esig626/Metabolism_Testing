# Phase 2 raw search results

This directory restores the raw-search location referenced by the Phase 2 scripts and audit records.

`B/` contains the visible Workstream B raw captures that are tracked individually. `phase2_raw_search_results.zip` preserves the larger uploaded archive exactly as received. The archive was not unpacked during the structural cleanup, so other workstream captures should be verified against it in the next inventory pass rather than assumed to be present or absent.
