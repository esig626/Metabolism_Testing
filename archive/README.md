# Archive

This directory contains historical migration material and preserved snapshots that are useful for provenance but are not presented as the current scientific state of the project.

`migrations/` contains the tracked migration directory, its report, and the uploaded migration ZIP. Historical corpus snapshots inside that material remain unchanged and should not be treated as the canonical current corpus without a separate verification step.
